{
  "name": "@pg-flux/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/dev.ts",
    "preview": "bun --hot scripts/serve-dist.ts"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "shiki": "^1.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
